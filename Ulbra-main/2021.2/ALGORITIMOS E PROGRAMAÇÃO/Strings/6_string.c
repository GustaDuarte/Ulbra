//FUP que leia uma string e a escreva caracter a caracter, intercalando letras maiúsculas e minúsculas. Por exemplo, lê aula escreve AuLa.

/*#include <stdio.h>
#include <ctype.h>
#include <string.h>

   int main(){
      char string[100];
      char novaString[30];

      printf("\nINFORME A STRING:\n");
      gets(string);
      strcpy(string, strlwr(string));
      for( int i = 0; string[i] != '\0'; i+2)
      {
         strcat(novaString,strdup (string[i]));
      }
         printf("%s", novaString);
     
      return 0;
   }*/

/*#include <stdio.h>
#include <math.h>
int main () {
   float x=5, y;
   y = x * 2;
   x = x + y * 2;
   x = int (x / y);
   y = x + y;
   x= y + x*2;
   printf ("%2.f", x);
   return 0;
}*/

/*#include <stdio.h>
#include <conio.h>
main () {
      int vl = 13;
   printf("\"Valor = %i\"\n%i + %i = %i", vl, vl, vl);
}*/

/*#include <stdio.h>
int main () {
   float valor = 136.4;
   printf("0%8.4f", valor);
   return 0;
}*/